$$

     int    low,  high, step;      [* declarations *]
     bool tests, are, fun;
     real yikes, we, are, done;

     get (my, favorite, color);
     while (low  <  high )  
         {  put (low-high+high+1+1.5);
             put (convert1x (low)+2);
             low =  low  +  step * 3 + (4 - low);
         } 
      whileend
      return 3 + (4 - 2);
$$